import type { Tuple } from "src/types/tuple";
import type { Stemmer } from "../../stemmer";


export default class ENStemmer implements Stemmer {

  //TODO: implement stem
  public stem(word: string): string {
    return word
  }
}
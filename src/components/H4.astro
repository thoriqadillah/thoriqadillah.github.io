---
import type { HTMLAttributes } from 'astro/types'

const textcolor = {
  'second': 'text-second',
  'main': 'text-main',
  'dark': 'text-dark',
  'black': 'text-black',
  'bright': 'text-bright',
}

interface Props extends HTMLAttributes<'h4'> {
  color: keyof typeof textcolor
  classname?: string
}

const { color, classname, ...props } = Astro.props

const style = `text-xl font-bold ${ textcolor[color] } ${ classname }`
---

<h4 {...props} class={ style }>
  <slot/>
</h4>
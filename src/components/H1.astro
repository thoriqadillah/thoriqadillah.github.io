---
import type { HTMLAttributes } from 'astro/types'

const textcolor = {
  'second': 'text-second',
  'main': 'text-main',
  'dark': 'text-dark',
  'black': 'text-black',
  'bright': 'text-bright',
}

interface Props extends HTMLAttributes<'h1'> {
  color: keyof typeof textcolor
  classname?: string
}

const { color, classname, ...props } = Astro.props

const style = `text-6xl font-black ${ textcolor[color] } ${ classname }`
---

<h1 {...props} class={ style }>
  <slot/>
</h1>